# Example CLI with Python(Click)
## How to use
- Clone the repo
- Create an virtual environment:
  ```bash
  virtualenv venv && source venv/bin/activate
  ```
- Install program:
  ```bash
  pip install --editable .
  ```
- Start using CLI:
  ```bash
  person work program
  ```

